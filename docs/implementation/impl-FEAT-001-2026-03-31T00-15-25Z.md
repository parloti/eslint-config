# ⚙️ Implementation: Remove disabledPlugins And Centralize Plugin State

**🐞 Bug:** BUG-001 - Remove the legacy disable-only plugin option
**🏛️ Epic:** [EPIC-001](../product/epics/epic-001-plugin-configuration-controls.md) - Plugin Configuration Controls
**🚀 Feature:** [FEAT-001](../product/features/feature-001-plugin-state-overrides.md) - Plugin State Overrides
**🧾 User Story:** [US-001](../product/user-stories/us-001-override-plugin-default-state.md) - Override plugin default state
**🕒 Created At:** 2026-03-31T00:15:25Z
**📌 Status:** Ready for review

---

## 🎯 Objective

Finish the review follow-up by removing the public `disabledPlugins` symbol, moving plugin-state behavior into one shared runtime module, and aligning public docs with the plugins-only API.

---

## 🧱 Changes

- File: src/types.ts
  - Removed the legacy `disabledPlugins` option from `ConfigOptions`
- File: src/plugin-state.ts
  - Added the shared plugin-state runtime for defaults, overrides, and redundant-override warnings
- File: src/config-factory.ts
  - Switched config composition to use the shared plugin-state resolver
- File: src/plugin-state.spec.ts
  - Added a companion spec covering default-disabled plugins and redundant warning behavior
- File: README.md
  - Updated public examples and behavior notes to the `plugins` API
- File: docs/api/index/interfaces/ConfigOptions.md
  - Updated the checked-in API docs to remove the deleted option

---

## 💻 Code

```ts
const isEnabled = resolvePluginState(pluginName, options.plugins);

if (isEnabled) {
  loaders.push(loadPluginConfig(pluginName, loadConfig, loaderEntry.mode));
}
```

---

## 🧪 Notes

- Edge cases handled: redundant enable and redundant disable overrides still emit warnings
- Reproduction/regression checks: package composition still omits `jasmine` and `jest` by default, but explicit opt-in continues to work
- Assumptions: the checked-in API docs are part of the maintained public surface and should change with the code
- Limitations: review history still references `disabledPlugins` as historical context
- Validation performed: `npm run validate` twice, both successful

## 🔁 Handoff

- Next: Reviewer Agent / human
- Status: ready-for-handoff
